export const APP_NAME = "XRPL Token Insurance Dashboard";

/** No-op function. Use to intentionally ignore promise rejections, e.g. `promise.catch(noop)`. */
export function noop(): void {
  // Intentionally empty
}
